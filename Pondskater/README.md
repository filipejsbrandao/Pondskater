# Pondskater

## Weights

- Multiplicative weights use `w` and scale the base wavefront speed.
- Additive weights use `wa` and shift the base wavefront speed: `effective = 1 + wa`.
- Additive and multiplicative modes are mutually exclusive in the input.
